// Calling the function before its declaration
console.log(multiplyNumber(5, 3)); // Output: 15

// Function expression to define multiplyNumber
var multiplyNumber = function(a, b) {
    return a * b;
};
